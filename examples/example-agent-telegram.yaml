apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-telegram-agent-claude-data
  namespace: claude-agents
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-telegram-agent
  namespace: claude-agents
  labels:
    app.kubernetes.io/part-of: claude-agent-farm
    platform: telegram
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-telegram-agent
  template:
    metadata:
      labels:
        app: my-telegram-agent
        platform: telegram
    spec:
      containers:
        - name: claude-code
          image: claude-agent-telegram:latest
          imagePullPolicy: Never
          tty: true
          stdin: true
          env:
            - name: TELEGRAM_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: telegram-bot-token
                  key: TELEGRAM_BOT_TOKEN
            - name: TELEGRAM_USER_ID
              valueFrom:
                secretKeyRef:
                  name: telegram-bot-token
                  key: TELEGRAM_USER_ID
            - name: TELEGRAM_CHAT_ID
              value: "your-chat-id"
            - name: AGENT_SYSTEM_PROMPT
              value: "You are a helpful assistant."
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: claude-data
              mountPath: /home/agent/.claude
            - name: claude-credentials
              mountPath: /secrets/claude-config
              readOnly: true
          livenessProbe:
            exec:
              command: ["pgrep", "-f", "claude"]
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: claude-data
          persistentVolumeClaim:
            claimName: my-telegram-agent-claude-data
        - name: claude-credentials
          secret:
            secretName: claude-config
            optional: true
